[color]
        ui = auto
[diff]
	external = sh -c 'vimdiff $2 $5' git_diff_wrapper
[pager]
	diff =
[push]
	default = simple
[core]
        autocrlf = input
[pull]
#	ff = only
	ff =
[alias]
	up = "!git remote update -p; git merge --ff-only @{u}"
[credential]
#        helper = store --file /run/user/$(id -u)/git-credentials
#        helper = store --file ~/.git-credentials
        helper = cache --timeout 300
